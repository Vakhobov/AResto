/**
 * branchUtils.ts
 * ──────────────
 * Lightweight compatibility shim.
 * The new auth system uses Firebase Auth + Firestore profiles.
 * This file is kept for any legacy imports but is largely empty.
 */

// No-op placeholder — session management is now handled by Firebase Auth (AuthContext.tsx)
// and userProfile is accessed via useAuth().userProfile
